

const ParameterBox = (props) => {
    // effect would have to equal ping pong 
    const effect = props.effect

    return (
        <>
        <div className="params-box">
            effect.openParameters()
        </div>
        </>
    )
}

export default ParameterBox